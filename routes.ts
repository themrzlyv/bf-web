export const publicRoutes = ["/"];

export const authRoutes = ["/", "/register"];

export const protectedRoutes = ["/user", "/user/profile"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT_URL = "/user";
